<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyf.mallproductservice.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.hyf.mallproductservice.entity.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <result property="totalStock" column="total_stock"/>
        <result property="remainStock" column="remain_stock"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, price, total_stock, remain_stock, status, create_time, update_time
    </sql>

    <!-- 插入商品 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (
            category_id, name, price, total_stock, remain_stock, status
        ) VALUES (
            #{categoryId}, #{name}, #{price}, #{totalStock}, #{remainStock}, #{status}
        )
    </insert>

    <!-- 根据ID删除商品 -->
    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <!-- 动态更新商品 -->
    <update id="update">
        UPDATE product
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="price != null">price = #{price},</if>
            <if test="totalStock != null">total_stock = #{totalStock},</if>
            <if test="remainStock != null">remain_stock = #{remainStock},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product
        WHERE id = #{id}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product
        ORDER BY create_time DESC
    </select>

    <!-- 扣减商品库存（安全且带悲观/乐观防御，防负库存） -->
    <update id="decreaseStock">
        UPDATE product
        SET remain_stock = remain_stock - #{count}
        WHERE id = #{id} AND remain_stock >= #{count}
    </update>

</mapper>
