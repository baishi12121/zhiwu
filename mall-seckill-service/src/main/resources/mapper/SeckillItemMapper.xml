<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyf.mallseckillservice.mapper.SeckillItemMapper">

    <select id="selectEnabledByActivityId" resultType="com.hyf.mallseckillservice.entity.SeckillItemDO">
        SELECT *
        FROM seckill_item
        WHERE activity_id = #{activityId}
          AND status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <update id="deductStock">
        UPDATE seckill_item
        SET seckill_stock = seckill_stock - #{quantity}
        WHERE id = #{id}
          AND seckill_stock &gt;= #{quantity}
    </update>

    <update id="restoreStock">
        UPDATE seckill_item
        SET seckill_stock = seckill_stock + #{quantity}
        WHERE id = #{id}
    </update>
</mapper>
