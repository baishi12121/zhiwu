<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyf.mallorderservice.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.hyf.mallorderservice.entity.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="couponId" column="coupon_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="realAmount" column="real_amount"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, coupon_id, total_amount, real_amount, status, create_time, update_time
    </sql>

    <!-- 插入订单：order_no 由 SQL 自动生成（yyyyMMddHHmmss + 6位userId） -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (
            order_no, user_id, coupon_id, total_amount, real_amount, status
        ) VALUES (
            CONCAT(DATE_FORMAT(NOW(), '%Y%m%d%H%i%s'), LPAD(#{userId}, 6, '0')),
            #{userId}, #{couponId}, #{totalAmount}, #{realAmount}, #{status}
        )
    </insert>

    <!-- 根据ID删除订单 -->
    <delete id="deleteById">
        DELETE FROM `order` WHERE id = #{id}
    </delete>

    <!-- 动态更新订单 -->
    <update id="update">
        UPDATE `order`
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="couponId != null">coupon_id = #{couponId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="realAmount != null">real_amount = #{realAmount},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        WHERE id = #{id}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID查询 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>
